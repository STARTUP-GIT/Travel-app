import React from 'react';
import HomeScreen from '../../app/(tabs)/index';

/**
 * PathTrackerView is the primary reusable view component for the Path Tracker feature.
 * It can be embedded as a tab or screen in any React Native or Expo application.
 */
export default function PathTrackerView() {
  return <HomeScreen />;
}
